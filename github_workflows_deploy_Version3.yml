name: Deploy (Placeholder)

on:
  workflow_dispatch:
  workflow_run:
    workflows: ["CI"]
    types: [completed]

jobs:
  deploy:
    if: ${{ github.event_name == 'workflow_dispatch' || (github.event.workflow_run.conclusion == 'success') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install
        run: npm install

      - name: Build
        run: |
          if npm run | grep -q build; then
            npm run build
          else
            echo "Kein build Skript."
          fi

      - name: (Placeholder) Deployment
        run: echo "Hier später Deployment integrieren (z.B. vercel --prod / Pages Upload)."